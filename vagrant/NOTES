NOTES:
If using the django dev-server, it must be invoked as “python manage.py runserver 0.0.0.0:8000" for it to be accessible outside the vm
Database details: django:django@127.0.0.1:5432 (superuser account exists for vagrant user)